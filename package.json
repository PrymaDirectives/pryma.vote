{
  "name": "pryma-poll",
  "version": "0.1.0",
  "description": "Privacy-first public polling form for PRYMA governance input",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "aggregate": "node scripts/aggregate.js",
    "test": "node scripts/aggregate.js --dry-run"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
